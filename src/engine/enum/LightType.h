#ifndef PROJECTION_LIGHTTYPE_H
#define PROJECTION_LIGHTTYPE_H
namespace PEngine {
    enum LightType {
        DIRECTIONAL,
        SPOT,
        POINT,
        SPHERE,
        DISK,
        PLANE,
    };
}
#endif
